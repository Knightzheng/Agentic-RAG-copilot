# 2026-04-08 阶段三高质量 RAG

## 目标

- 打通 `Milestone 3：高质量 RAG`
- 完成 dense retrieval、lexical retrieval、hybrid merge、rerank、evidence grade、citations 的最小闭环
- 把前端从阶段一骨架升级到可直接演示的 Chat Workspace 基础版

## 实施内容

- 接入 `document_embeddings`、`document_fts`、`agent_runs`、`retrieval_runs`、`retrieval_candidates`、`rerank_results`、`citations`、`evidence_assessments`
- 启用 DashScope 客户端，按项目约定使用：
  - `qwen3-max`
  - `text-embedding-v4`
  - `qwen3-rerank`
- 完成 `POST /api/chat`
- 完成线程详情查询 `GET /api/threads/{thread_id}`
- 完成文档重建索引接口 `POST /api/documents/{document_id}/reindex`
- 前端补齐 Chat Workspace 最小交互、引用展示和手动重建索引按钮

## 稳态修复

- 索引构建改为“FTS 始终可构建，embedding 依赖模型 key”，避免新文档在缺少 embedding key 时完全不可检索
- 文档上传后统一进入索引构建流程，不再只在有 key 时才建索引
- 前端补充问答失败提示，避免接口报错时静默失败

## 验证结果

- 已确认数据库迁移版本为 `20260408_0002`
- 已确认以下表存在：
  - `app_kb.document_embeddings`
  - `app_kb.document_fts`
  - `app_agent.agent_runs`
  - `app_agent.retrieval_runs`
  - `app_agent.retrieval_candidates`
  - `app_agent.rerank_results`
  - `app_agent.citations`
  - `app_agent.evidence_assessments`
- 已实测 `POST /api/chat` 成功返回答案、证据等级和引用
- 已抽样运行 5 条 easy QA，结果均返回 `sufficient` 且带引用

## 本轮抽样

- Q001：五个一级模块，返回正确
- Q002：Starter 可连接 MCP Server 数量，返回正确
- Q003：Business 单文件大小上限，返回正确
- Q004：Enterprise 默认日志保留天数，返回正确
- Q005：Viewer 是否可上传文档，返回正确

## 当前边界

- OCR 仍未落地，扫描件 PDF 目前只具备识别和降级标记
- SSE、Trace 可视化、评测看板还未进入本轮范围
- 还没有把 75 条 QA 批量接成自动评测脚本

## 下一步建议

1. 进入 LangGraph 编排阶段，开始做 `Milestone 4`
2. 增加批量评测脚本，对 75 条 QA 自动统计 Recall、Citation Precision、Answer Accuracy
3. 为 Chat 页面补线程详情回看、引用跳转和 chunk 侧栏
