# 2026-04-08 第二阶段高质量 RAG

## 目标

- 完成 embedding 入库与全文检索索引
- 落地 hybrid retrieval、rerank、evidence grading
- 接通 `POST /api/chat` 的同步问答链路
- 让前端可以发起基础问答并查看引用

## 本次新增

- 新增第二阶段数据库迁移：
  - `app_kb.document_embeddings`
  - `app_kb.document_fts`
  - `app_agent.agent_runs`
  - `app_agent.retrieval_runs`
  - `app_agent.retrieval_candidates`
  - `app_agent.rerank_results`
  - `app_agent.citations`
  - `app_agent.evidence_assessments`
- 新增百炼客户端：
  - embedding
  - qwen3-rerank
  - qwen3-max
- 新增索引构建服务：
  - child chunk embedding 入库
  - FTS 同步
  - 版本状态更新
- 新增检索服务：
  - dense retrieval
  - lexical retrieval
  - RRF merge
  - parent context 扩展
  - rerank
- 新增聊天服务：
  - 创建线程
  - 记录 run
  - grounded answer
  - citation 绑定
- 前端 Chat Workspace 已接入基础问答与引用展示

## 验证结果

- 已执行 Alembic 第二阶段迁移并成功升级到 `20260408_0002`
- 已用真实百炼模型完成现有 4 个文档的 reindex
- 实测 embedding 入库数量：
  - `atlas_kb_test_corpus_v1.md`：`16`
  - `lightrag_2410.05779.pdf`：`16`
  - `optional_syllabus_template_sample.docx`：`30`
  - `hinton_nn_lecture12.pptx`：`25`
- 已用真实模型验证 `POST /api/chat`
- 已验证 `GET /api/threads/{thread_id}` 可读回消息列表
- 后端单测仍通过：`2 passed`
- 前端构建通过：`npm run build`

## 当前结果说明

- 当前 `POST /api/chat` 已能返回 grounded answer 和 citations
- 对测试问题“Atlas 平台的五个一级模块分别是什么？”已经能返回正确主体结论
- 当前回答仍有轻微冗余，后续可继续优化 prompt、citation 选择和 evidence grade 阈值

## 下一步建议

1. 用 `atlas_kb_test_qa_v1.json` 批量跑离线评测，统计 Recall@K、Citation Precision、Answer Accuracy
2. 为 Chat 页补线程切换、历史消息回显和右侧 citation inspector
3. 继续推进 SSE 流式事件和 run trace 查询接口
