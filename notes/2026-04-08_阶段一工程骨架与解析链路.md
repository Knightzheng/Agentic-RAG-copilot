# 2026-04-08 阶段一工程骨架与解析链路

## 目标

- 按设计文档建立第一阶段工程骨架
- 建立独立虚拟环境约定
- 完成本地 PostgreSQL 基础迁移
- 落地最小文档接入、解析、切块主链路
- 搭出前端最小页面骨架

## 实施内容

- 创建 `backend/`、`frontend/`、`notes/` 目录
- 创建 FastAPI 应用入口、路由分层、配置模块、数据库会话模块
- 创建 `app_core`、`app_kb`、`app_agent` 第一阶段核心表的 Alembic 迁移
- 创建本地数据库清理脚本 `backend/app/scripts/reset_local_db.py`
- 实现 Markdown / PDF / DOCX / PPTX 四类文件的最小解析器
- 实现 parent-child chunk 基础切分服务
- 实现文档上传、解析、切块、文档/块查询接口
- 创建 React + TypeScript 前端骨架与知识库上传页面

## 验证结果

- 已创建项目虚拟环境：`.venv/`
- 后端依赖已安装完成，`pytest backend/tests` 通过，结果：`2 passed`
- 前端依赖已安装完成，`npm run build` 通过
- 已按要求清理 `rag_lab`
- 已执行 Alembic 迁移，第一阶段表成功创建
- 已用实际文件完成接入验证：
  - `atlas_kb_test_corpus_v1.md`
  - `lightrag_2410.05779.pdf`
  - `optional_syllabus_template_sample.docx`
  - `hinton_nn_lecture12.pptx`
- 实测入库结果：
  - 文档数：`4`
  - chunk 数：`158`
- 已用 FastAPI TestClient 验证：
  - `GET /api/healthz`
  - `GET /api/workspaces`
  - `POST /api/documents/upload`

## 当前已知边界

- 扫描件 PDF 已具备识别和降级标记能力，但当前环境没有 OCR 工具，因此还没有真正跑 OCR 兜底
- embedding / FTS / retrieval / rerank 仍是下一阶段内容，本阶段只做到上传、解析、切块和状态查询
- 前端当前为第一阶段骨架页，不包含 Chat SSE、Trace、Memory、MCP 的完整交互

## 下一步建议

1. 继续做第二阶段的 `document_embeddings`、`document_fts`、检索服务和引用回答接口
2. 为上传链路补更多详情查询与前端联调
3. 把测试集 `75` 条 QA 接入离线评测脚本，形成最小可量化验收
